// src/api/booking.ts
import type { Booking } from '../types'

export async function fetchBookingDetails(bookingId: string): Promise<Booking> {
  // Replace with your actual endpoint; for now, throw an error or return a mock.
  throw new Error('Not implemented: Use station.bookings for demo data.')
}
